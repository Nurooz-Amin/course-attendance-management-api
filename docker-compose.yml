services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - sqlite_data:/app/data
    environment:
      DB_NAME: data/db.sqlite3

volumes:
  sqlite_data:
